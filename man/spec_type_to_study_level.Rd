% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spec_builder.R
\name{spec_type_to_study_level}
\alias{spec_type_to_study_level}
\title{Spec to study_level}
\usage{
spec_type_to_study_level(
  doc,
  cols = c(study_name = "[Ss]tudy.?[Nn]ame", study_description =
    "[Ss]tudy.?[Dd]escription", protocol_name = "[Pp]rotocol.?[Nn]ame", standard_name =
    "[Ss]tandard.?[Nn]ame", standard_version = "[Ss]tandard.?[Vv]ersion", define_version
    = "[Dd]efine.?[Vv]ersion", language = "[Ll]anguage"),
  sheet = "[Dd]efine|[Ss]tudy",
  pivot = TRUE
)
}
\arguments{
\item{doc}{Named list of data frames produced by \code{\link[=read_all_sheets]{read_all_sheets()}}.}

\item{cols}{Named character vector of regular expressions.
\itemize{
\item When \code{pivot = TRUE} (default): regexes are matched against the \strong{pivoted
attribute values} (which become column names after the pivot). Any
attribute not matched is silently discarded; missing schema columns are
filled with \code{NA}.
\item When \code{pivot = FALSE}: regexes are matched against the \strong{sheet's column
headings}, identical to other \verb{spec_type_to_*} builders. Only the
columns listed in \code{cols} are required; missing schema columns are filled
with \code{NA}.
}

Names must be a subset of the \code{study_level} schema column names:
\code{study_name}, \code{study_description}, \code{protocol_name}, \code{standard_name},
\code{standard_version}, \code{define_version}, \code{language}. A minimum of
\code{study_name}, \code{study_description}, and \code{protocol_name} should be supplied.}

\item{sheet}{Regular expression used to identify the study-level sheet.
Defaults to matching sheets named \code{"Define"} or \code{"Study"}.}

\item{pivot}{Logical; when \code{TRUE} (default) the sheet is assumed to be in
attribute–value format and is pivoted to a single wide row before column
mapping. Set to \code{FALSE} when the sheet already stores study metadata as
named columns.}
}
\value{
A one-row tibble formatted for the \code{study_level} table of a
\code{MetacoreDefine} object.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}
Builds the \code{study_level} table from a named list of Excel sheets produced by
\code{\link[=read_all_sheets]{read_all_sheets()}}. \code{study_level} holds \strong{one row of study-wide metadata}
used when generating Define.xml 2.0 output.
}
\details{
\subsection{How it works}{

The function locates a sheet whose name matches \code{sheet} (typically \code{"Define"}
or \code{"Study"}). It then either \strong{pivots} the sheet (the default) or reads
columns directly, depending on \code{pivot}.
}

\subsection{Pivoted format (\code{pivot = TRUE}, default)}{

The sheet contains two columns: one whose heading matches \verb{[Aa]ttribute}
(holding the field names) and one whose heading matches \verb{[Vv]alue} (holding
the corresponding values). For example:\tabular{ll}{
   Attribute \tab Value \cr
   StudyName \tab CDISC Pilot \cr
   StudyDescription \tab Phase III efficacy... \cr
   ProtocolName \tab CDISCPILOT01 \cr
}


The function pivots this into a single-row tibble with one column per
attribute, then renames columns to the standard schema names using the
regular expressions in \code{cols}. Any attribute not matched by a \code{cols} entry is
discarded; any schema column not found in the sheet is filled with \code{NA}.
}

\subsection{Columnar format (\code{pivot = FALSE})}{

The sheet already stores each field as its own column (one row of data). The
regexes in \code{cols} are matched against the \strong{column headings} of the sheet
directly, and the matching columns are selected and renamed. This is the
same approach used by all other \verb{spec_type_to_*} builders.
}

\subsection{Output columns}{\tabular{ll}{
   Column \tab Description \cr
   \code{study_name} \tab Short study identifier \cr
   \code{study_description} \tab Human-readable study description \cr
   \code{protocol_name} \tab Protocol number or name \cr
   \code{standard_name} \tab CDISC standard name (e.g. \code{"ADaM"}) \cr
   \code{standard_version} \tab Version of the standard (e.g. \code{"1.0"}) \cr
   \code{define_version} \tab Define-XML version (e.g. \code{"2.0"}) \cr
   \code{language} \tab Language code (e.g. \code{"en"}) \cr
}

}
}
\seealso{
Other spec builders:
\code{\link[=spec_type_to_codelist]{spec_type_to_codelist()}},
\code{\link[=spec_type_to_comments]{spec_type_to_comments()}},
\code{\link[=spec_type_to_derivations]{spec_type_to_derivations()}},
\code{\link[=spec_type_to_documents]{spec_type_to_documents()}},
\code{\link[=spec_type_to_ds_spec]{spec_type_to_ds_spec()}},
\code{\link[=spec_type_to_ds_vars]{spec_type_to_ds_vars()}},
\code{\link[=spec_type_to_supp]{spec_type_to_supp()}},
\code{\link[=spec_type_to_value_spec]{spec_type_to_value_spec()}},
\code{\link[=spec_type_to_var_spec]{spec_type_to_var_spec()}}
}
\concept{spec builders}
