% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/define_xml_writers.R
\name{metacore_to_define_xml}
\alias{metacore_to_define_xml}
\title{Convert Metacore Object to Define.xml 2.0}
\usage{
metacore_to_define_xml(
  metacore_obj,
  path = NULL,
  study_details = NULL,
  timestamp = Sys.time(),
  source_system = "R metacore package",
  source_system_version = packageVersion("metacore"),
  stylesheet_href = "define2-0-0.xsl",
  validate = TRUE
)
}
\arguments{
\item{metacore_obj}{A Metacore object containing dataset, variable, and metadata
specifications. See \link{Metacore} for details on required tables and structure.}

\item{path}{Optional file path where the Define.xml should be written. If \code{NULL},
the XML document is returned in memory but not written to disk. Default: \code{NULL}}

\item{study_details}{Required named list containing study-level metadata for the
Define.xml root elements. Must include at minimum:
\describe{
\item{study_name}{Character. The name of the study (e.g., "STUDY ABC-123")}
\item{study_description}{Character. A description of the study}
\item{protocol_name}{Character. The protocol identifier (e.g., "ABC-123-001")}
}
Optional elements:
\describe{
\item{study_oid}{Character. OID for the Study element. If not provided, will
be auto-generated.}
\item{metadata_version_oid}{Character. OID for MetaDataVersion. If not provided,
will be auto-generated.}
\item{metadata_version_name}{Character. Name attribute for MetaDataVersion. If not
provided, defaults to "Metadata v1.0".}
}}

\item{timestamp}{POSIXct or character. The creation timestamp for the Define.xml
file in ISO 8601 format. If POSIXct, will be converted to ISO 8601 with timezone.
Default: \code{Sys.time()} (current system time)}

\item{source_system}{Character. Identifier for the system that created the Define.xml.
Default: \code{"R metacore package"}}

\item{source_system_version}{Character. Version of the source system. Default uses
the installed metacore package version via \code{packageVersion("metacore")}.}

\item{stylesheet_href}{Character. URL or path to the XSL stylesheet for rendering
the Define.xml in a browser. Default: \code{"define2-0-0.xsl"}}

\item{validate}{Logical. If \code{TRUE}, runs validation checks on the metacore object
and generated XML before returning. Default: \code{TRUE}}
}
\value{
Invisibly returns an xml_document object (from the xml2 package). If \code{path}
is provided, the XML is also written to that location.
}
\description{
Generates a CDISC Define.xml 2.0 document (ODM 1.3.2 with def: namespace) from a
metacore object. The function combines dataset, variable, value-level, codelist, and
method metadata from the metacore object with user-provided study details to create
a complete, valid Define.xml file.
}
\details{
\subsection{XML Structure Generated}{

The function generates the following Define.xml structure:
\itemize{
\item \strong{ODM root element} with appropriate namespaces (odm, def, xlink)
\item \strong{Study element} with OID and metadata
\item \strong{GlobalVariables} containing study name, description, and protocol
\item \strong{MetaDataVersion} with Define version and standards information
\item \strong{ItemGroupDef elements} (one per dataset from \code{ds_spec})
\item \strong{ItemDef elements} (one per unique variable)
\item \strong{ItemRef elements} linking variables to datasets with ordering and key sequences
\item \strong{def:WhereClauseDef elements} for conditional value-level metadata
\item \strong{def:ValueListDef elements} grouping related where clauses
\item \strong{CodeList and ExternalCodeList elements} for value lists and external dictionaries
\item \strong{MethodDef elements} for derivations with document references
\item \strong{def:CommentDef elements} for variable comments
\item \strong{def:DocumentRef and def:leaf elements} for linked documents
}
}

\subsection{Study Details Requirements}{

The \code{study_details} parameter is critical for proper Define.xml generation. At minimum,
provide:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{study_details <- list(
  study_name = "STUDY ABC-123",
  study_description = "A Phase 3 Randomized, Double-Blind Study",
  protocol_name = "ABC-123-001"
)
}\if{html}{\out{</div>}}
}

\subsection{OID Generation Strategy}{

The function uses deterministic OID generation based on CDISC conventions:
\itemize{
\item ItemGroupDef: \verb{IG.\{DOMAIN\}}
\item ItemDef: \verb{IT.\{DOMAIN\}.\{VARIABLE\}} for simple variables
\item ItemDef with where clause: \verb{IT.\{DOMAIN\}.\{VARIABLE\}.\{HASH\}} where HASH represents
the specific where clause condition
\item CodeList: \verb{CL.\{CODE_ID\}}
\item MethodDef: \verb{MT.\{DERIVATION_ID\}}
\item Study: Auto-generated or user-provided via \code{study_details$study_oid}
}

All OIDs are verified for uniqueness before XML generation.
}

\subsection{Timestamp Handling}{

The \code{timestamp} parameter is included in the ODM element as \code{CreationDateTime} in
ISO 8601 format with timezone information. This is required by the Define.xml specification.
}

\subsection{Validation}{

When \code{validate = TRUE}, the function performs:
\enumerate{
\item \strong{Metacore validation}: Ensures all required tables are present and valid
\item \strong{Study details validation}: Checks that required study metadata is provided
\item \strong{XML structure validation}: Verifies all generated elements have required attributes
\item \strong{OID uniqueness}: Confirms no duplicate OIDs exist in the generated document
\item \strong{Namespace correctness}: Validates namespace declarations and usage
}

If validation fails, an informative error message is returned with details on
what needs to be corrected.
}

\subsection{File Output}{

If \code{path} is provided, the XML is written using \code{\link[xml2:write_xml]{xml2::write_xml()}} with UTF-8
encoding and proper XML declaration. The file can be validated against the
CDISC Define.xml 2.0 XSD schema.
}
}
\examples{
\dontrun{
# Load a metacore object from a P21 specification
metacore_obj <- spec_to_metacore(
  path = "path/to/specification.xlsx"
)

# Define study metadata
study_details <- list(
  study_name = "STUDY ABC-123",
  study_description = "A Phase 3 Randomized Study",
  protocol_name = "ABC-123-001"
)

# Generate Define.xml and write to file
xml_doc <- metacore_to_define_xml(
  metacore_obj = metacore_obj,
  path = "output/define.xml",
  study_details = study_details
)

# Or generate in memory without writing to file
xml_doc <- metacore_to_define_xml(
  metacore_obj = metacore_obj,
  study_details = study_details
)
}

}
\seealso{
\itemize{
\item \link{Metacore} for creating and validating metacore objects
\item \code{\link[=spec_to_metacore]{spec_to_metacore()}} for reading P21 Excel specifications
\item \code{\link[=define_to_metacore]{define_to_metacore()}} for the inverse operation (reading Define.xml)
}
}
