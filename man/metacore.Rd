% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Metacore.R
\name{metacore}
\alias{metacore}
\title{R6 Class wrapper to create your own metacore object}
\usage{
metacore(
  ds_spec = NULL,
  ds_vars = NULL,
  var_spec = NULL,
  value_spec = NULL,
  derivations = NULL,
  codelist = NULL,
  supp = NULL,
  study_level = NULL,
  documents = NULL,
  comments = NULL,
  define_fields = FALSE,
  quiet = deprecated(),
  verbose = "message"
)
}
\arguments{
\item{ds_spec}{contains each dataset in the study, with the labels for each}

\item{ds_vars}{information on what variables are in each dataset + plus dataset specific variable information}

\item{var_spec}{variable information that is shared across all datasets}

\item{value_spec}{parameter specific information, as data is long the specs for wbc might be difference the hgb}

\item{derivations}{contains derivation, it allows for different variables to have the same derivation}

\item{codelist}{contains the code/decode information}

\item{supp}{contains the idvar and qeval information for supplemental variables}

\item{study_level}{contains study-level metadata used when generating a
Define.xml (study name, description, protocol, standard and Define-XML
versions, language). Optional; a single-row table.}

\item{documents}{contains references to external documents (e.g. the Analysis
Data Reviewer's Guide) used for Define.xml page references. Optional; one
row per document with \code{document_id}, \code{title} and \code{href}.}

\item{comments}{contains variable comments used for Define.xml CommentDef
elements. Optional; one row per comment with \code{comment_id} and \code{comment}.
Links to variables via \code{value_spec$comment_id}.}

\item{define_fields}{logical; when \code{TRUE} the object includes the extended
columns required for Define.xml generation (\code{class}, \code{repeating},
\code{reference}, \code{purpose} in \code{ds_spec}; \code{role} in \code{ds_vars}; \code{where_label}
and \code{comment_id} in \code{value_spec}; \code{method_name}, \code{method_type},
\code{document_id}, \code{pages} in \code{derivations}) as well as the \code{study_level},
\code{documents}, and \code{comments} tables. When \code{FALSE} (default) those fields
are stripped so the object matches the original metacore schema.}

\item{quiet}{\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#superseded}{\figure{lifecycle-superseded.svg}{options: alt='[Superseded]'}}}{\strong{[Superseded]}} Option to quietly load in, this
will suppress warnings, but not errors. Expects either \code{TRUE} or \code{FALSE}.
Default behaviour is \code{FALSE}. As of v0.3.0 this argument is deprecated in favour
of \code{verbose}.}

\item{verbose}{A character string specifying the desired verbosity level.
Must be one of:
\describe{
\item{"message"}{ (default) Messages and warnings are handled normally.}
\item{"warn"}{Messages are suppressed, but warnings are allowed.}
\item{"collapse"}{Warnings are collapsed into a single message indicating the
number of suppressed warnings.}
\item{"silent"}{Both messages and warnings are suppressed.}
}}
}
\description{
R6 Class wrapper to create your own metacore object
}
\concept{Metacore}
